fgets to an address of 100 to an address of 60, overflow possible

ltrace will give us the address where the fgets is pointing to

we can setup a trap with a shellcode

padding is 80 byte

21 shell + (80 - 21 byte padding) + 4 byte
or
21 shell + (59 byte padding) + 4 byte